/* ---------------------------------------------------- */
/*  Generated by Enterprise Architect Version 17.1 		*/
/*  Created On : 14-jul-2026 14:19:05 				*/
/*  DBMS       : PostgreSQL 						*/
/* ---------------------------------------------------- */

/* Drop Tables */

DROP TABLE IF EXISTS contact CASCADE
;

DROP TABLE IF EXISTS contactrole CASCADE
;

DROP TABLE IF EXISTS customer CASCADE
;

/* Create Tables */

CREATE TABLE contact
(
	id uuid NOT NULL,
	name varchar(200) NOT NULL,
	email varchar(254) NOT NULL,
	phone varchar(50) NULL,
	opt_in boolean NOT NULL,
	opt_in_date timestamp with time zone NULL,
	role varchar(30) NOT NULL,
	customer_id uuid NOT NULL,
	created_at timestamp with time zone NOT NULL,
	updated_at timestamp with time zone NOT NULL
)
;

CREATE TABLE contactrole
(
	code varchar(30) NOT NULL,
	label varchar(100) NOT NULL
)
;

CREATE TABLE customer
(
	id uuid NOT NULL,
	name varchar(200) NOT NULL,
	notes varchar(2000) NULL,
	address_mode varchar(10) NOT NULL,
	street_name varchar(200) NULL,
	house_number varchar(20) NULL,
	postal_code varchar(20) NULL,
	city varchar(100) NULL,
	country varchar(100) NULL,
	po_box varchar(200) NULL,
	created_at timestamp with time zone NOT NULL,
	updated_at timestamp with time zone NOT NULL,
	merged_into_id uuid NULL
)
;

/* Create Primary Keys, Indexes, Uniques, Checks */

ALTER TABLE contact ADD CONSTRAINT "PK_contact"
	PRIMARY KEY (id)
;

ALTER TABLE contact ADD CONSTRAINT "CK_contact_role" CHECK (role IN ('Primary','Purchase','Sales','License Holder','Secondary')
)
;

CREATE INDEX "IXFK_contact_contactrole" ON contact (role ASC)
;

CREATE INDEX "IXFK_contact_customer" ON contact (customer_id ASC)
;

ALTER TABLE contactrole ADD CONSTRAINT "PK_contactrole"
	PRIMARY KEY (code)
;

ALTER TABLE customer ADD CONSTRAINT "PK_customer"
	PRIMARY KEY (id)
;

CREATE INDEX "IXFK_customer_customer" ON customer (merged_into_id ASC)
;

/* Create Foreign Key Constraints */

ALTER TABLE contact ADD CONSTRAINT "FK_contact_contactrole"
	FOREIGN KEY (role) REFERENCES contactrole (code) ON DELETE No Action ON UPDATE No Action
;

ALTER TABLE contact ADD CONSTRAINT "FK_contact_customer"
	FOREIGN KEY (customer_id) REFERENCES customer (id) ON DELETE No Action ON UPDATE No Action
;

ALTER TABLE customer ADD CONSTRAINT "FK_customer_customer"
	FOREIGN KEY (merged_into_id) REFERENCES customer (id) ON DELETE No Action ON UPDATE No Action
;
