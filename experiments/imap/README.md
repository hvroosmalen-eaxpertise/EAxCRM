# IMAP Experiment

Goal: Connect to IMAP accounts, fetch unseen emails, and extract attachments.

Run:
    python -m experiments.imap.fetch_test

Test with a sample IMAP account before integrating into the CRM.
